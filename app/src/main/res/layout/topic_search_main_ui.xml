<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <include layout="@layout/title_search_layout" />

    <RelativeLayout
        android:id="@+id/search_history_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/dp_8"
        android:layout_marginRight="@dimen/dp_8"
        android:layout_marginTop="5dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/search_history_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:text="@string/search_doctor_history"
            android:textColor="@color/text_mid_color" />

        <TextView
            android:id="@+id/search_history_clear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:clickable="true"
            android:drawableLeft="@drawable/icon_delete_search_history"
            android:text="@string/clear_history"
            android:textColor="@color/text_mid_color" />

        <GridView
            android:id="@+id/search_topic_gridView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_below="@id/search_history_tv"
            android:layout_marginTop="6dp"
            android:columnWidth="200dp"
            android:gravity="center"
            android:horizontalSpacing="1dp"
            android:numColumns="2"
            android:stretchMode="columnWidth"
            android:verticalSpacing="5dp"></GridView>
    </RelativeLayout>

    <View style="@style/common_split_line" />

    <TextView
        android:id="@+id/search_topic_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="6dp"
        android:text="@string/search_topic_category"
        android:textColor="@color/text_mid_color" />

    <org.handmark.pulltorefresh.library.PullToRefreshListView xmlns:ptr="http://schemas.android.com/apk/res-auto"
        android:id="@+id/topic_search_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="10dp"
        android:cacheColorHint="#00000000"
        android:divider="@color/transparent"
        android:fadingEdge="none"
        android:scrollbars="none"
        ptr:headerType="normal"
        ptr:mode="both"></org.handmark.pulltorefresh.library.PullToRefreshListView>
</LinearLayout>
